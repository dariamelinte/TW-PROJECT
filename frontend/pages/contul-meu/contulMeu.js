import contulMeu from "../../components/contulMeu/contulMeu";
import header from '../../components/header.js';

const iconPath = '../../assets';

document.body.appendChild(header({ iconPath }));

document.body.appendChild(contulMeu());